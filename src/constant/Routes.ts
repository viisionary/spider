// show all pages in there
const IMAGES: string = '/images';
const ARTICLE: string = '/article';
const MEDIAS: string = '/medias';
const SOCKET: string = '/socket';

export {
    IMAGES,
    ARTICLE,
    MEDIAS,
    SOCKET
}
